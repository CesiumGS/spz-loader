{
  "name": "spz-loader",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "check": "pnpm -r --parallel run check",
    "check:write": "pnpm -r --parallel run check:write",
    "version:set": "pnpm exec lerna version",
    "build:cesium-wasm": "pnpm -F @cesium-spz-loader/core run build:docker",
    "publish:cesium-core": "NPM_CONFIG_PROVENANCE=true pnpm publish --filter \"@cesium-spz-loader/core\" --access public"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=8"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@lerna-lite/cli": "^3.10.0",
    "@lerna-lite/version": "^3.10.0"
  }
}
